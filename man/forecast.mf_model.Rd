% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forecast.R
\name{forecast.mf_model}
\alias{forecast.mf_model}
\alias{print.mf_model_forecast}
\title{Forecast a Mixed-Frequency Model}
\usage{
\method{forecast}{mf_model}(object, xreg = NULL, level = c(80, 95), ...)

\method{print}{mf_model_forecast}(x, ...)
}
\arguments{
\item{object}{A \code{"mf_model"} object returned by \code{\link[=mf_model]{mf_model()}}.}

\item{xreg}{Optional future regressors in a \code{\link[tsbox:ts_boxable]{tsbox::ts_boxable()}} format.
When omitted, the forecast regressor set stored inside \code{object} is used.
When supplied, \code{xreg} must contain the same non-target regressors used when
fitting the bridge equation.}

\item{level}{Prediction interval levels used when the model was estimated
with \code{se = TRUE}. When uncertainty is unavailable, \code{forecast()} still
returns the \code{se}, \code{lower}, and \code{upper} components, filled with \code{NA}.}

\item{...}{Unused.}

\item{x}{A \code{"mf_model_forecast"} object returned by \code{\link[=forecast.mf_model]{forecast.mf_model()}}.}
}
\value{
An object of class \code{"mf_model_forecast"} containing point forecasts,
predictive uncertainty summaries, the observed target history, the
target-period regressors used for forecasting, and optional full-system
bootstrap metadata.

\code{x}, invisibly.
}
\description{
Forecast the target variable from a fitted \code{\link[=mf_model]{mf_model()}} object.
}
\details{
In recursive bridge forecasts, uncertainty typically increases with
horizon because later forecast steps depend on forecasted rather than
observed target lags and, when needed, completed indicator paths. Under the
package's residual-resampling and full-system bootstrap workflows, those
simulated disturbances accumulate across steps, so standard errors and
interval widths can widen as the forecast horizon extends. The
\code{uncertainty-and-scenarios} vignette includes one worked example that trims
forecast rows by an acceptable prediction-interval width.
}
\section{Interoperability with the forecast package}{

\code{"mf_model_forecast"} deliberately does \emph{not} inherit from the \code{forecast}
package's \code{"forecast"} class. Target frequencies supported by \code{\link[=mf_model]{mf_model()}}
include daily, weekly and sub-daily series, which \code{\link[stats:ts]{stats::ts()}} cannot
represent without silently approximating the calendar, so an object that
claimed \code{"forecast"} inheritance could not honour it for every model this
package fits. Instead, \code{\link[=plot]{plot()}} and \code{\link[ggplot2:autoplot]{ggplot2::autoplot()}} methods are
provided directly for \code{"mf_model_forecast"}, and \code{\link[=as.forecast]{as.forecast()}} converts to
a genuine \code{"forecast"} object whenever the target frequency is regular
enough to allow it, for use with functions such as
\code{\link[forecast:reexports]{forecast::accuracy()}}.
}

\examples{
gdp_growth <- tsbox::ts_pc(gdp)
gdp_growth <- tsbox::ts_na_omit(gdp_growth)
model <- mf_model(
  target = gdp_growth,
  indic = baro,
  indic_predict = "auto.arima",
  indic_aggregators = "mean",
  h = 1
)

forecast(model)
}
