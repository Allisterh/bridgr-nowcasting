% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.mf_model_forecast}
\alias{plot.mf_model_forecast}
\alias{autoplot.mf_model_forecast}
\title{Plot a Mixed-Frequency Forecast}
\usage{
\method{plot}{mf_model_forecast}(
  x,
  level = NULL,
  history_n = 50,
  xlab = NULL,
  ylab = NULL,
  main = NULL,
  ...
)

\method{autoplot}{mf_model_forecast}(object, ...)
}
\arguments{
\item{x}{A \code{"mf_model_forecast"} object returned by \code{\link[=forecast.mf_model]{forecast.mf_model()}}.}

\item{level}{Prediction interval level to display. Must be one of the levels
the forecast was computed with. Defaults to the first available level.}

\item{history_n}{Number of historical target observations to display.
Defaults to the most recent \code{50}. Set to \code{NULL} to show the full history.}

\item{xlab, ylab, main}{Optional axis and title labels.}

\item{...}{Additional arguments passed to \code{\link[=theme_bridgr]{theme_bridgr()}}.}

\item{object}{A \code{"mf_model_forecast"} object, for the
\code{\link[ggplot2:autoplot]{ggplot2::autoplot()}} method.}
}
\value{
A ggplot2 object.
}
\description{
Visualize a \code{"mf_model_forecast"} object returned by \code{\link[=forecast.mf_model]{forecast.mf_model()}},
showing the observed target history together with the bridge forecast and,
when available, a prediction interval.
}
\details{
These methods are provided directly for \code{"mf_model_forecast"}
rather than inherited from the \pkg{forecast} package, so that plotting
works for every target frequency \code{\link[=mf_model]{mf_model()}} supports, including daily
and weekly targets that \code{\link[stats:ts]{stats::ts()}} cannot represent. See
\code{\link[=as.forecast]{as.forecast()}} to convert to a \code{"forecast"} object where the frequency
allows it.
}
\examples{
gdp_growth <- tsbox::ts_pc(gdp)
gdp_growth <- tsbox::ts_na_omit(gdp_growth)
model <- mf_model(
  target = gdp_growth,
  indic = baro,
  indic_predict = "auto.arima",
  indic_aggregators = "mean",
  h = 1
)

plot(forecast(model))
}
