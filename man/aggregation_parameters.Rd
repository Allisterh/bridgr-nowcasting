% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{aggregation_parameters}
\alias{aggregation_parameters}
\alias{aggregation_parameters.mf_model}
\title{Estimated Parametric Aggregation Parameters}
\usage{
aggregation_parameters(object, indicator = NULL, ...)

\method{aggregation_parameters}{mf_model}(object, indicator = NULL, ...)
}
\arguments{
\item{object}{A fitted \code{"mf_model"} object returned by \code{\link[=mf_model]{mf_model()}}.}

\item{indicator}{Optional indicator selector, either an indicator name or a
position. When supplied, the parameters for that single indicator are
returned directly; when \code{NULL} (default), a named list is returned.}

\item{...}{Unused.}
}
\value{
When \code{indicator} is \code{NULL}, a named list with one element per
indicator that used a parametric aggregator, each a numeric parameter
vector. When \code{indicator} is supplied, that single element, or \code{NULL} when
the indicator did not use a parametric aggregator.
}
\description{
Extracts the estimated parameters of parametric aggregation schemes
(\code{"expalmon"}, \code{"beta"}) from a fitted \code{\link[=mf_model]{mf_model()}} object. These are the
parameters whose implied weights are returned by \code{\link[=weights.mf_model]{weights.mf_model()}}.
}
\examples{
gdp_growth <- tsbox::ts_pc(gdp)
gdp_growth <- tsbox::ts_na_omit(gdp_growth)
model <- mf_model(
  target = gdp_growth,
  indic = baro,
  indic_aggregators = "expalmon",
  h = 1
)

aggregation_parameters(model)
}
\seealso{
\code{\link[=weights.mf_model]{weights.mf_model()}} for the implied aggregation weights.
}
