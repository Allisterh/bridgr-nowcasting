% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{weights.mf_model}
\alias{weights.mf_model}
\title{Aggregation Weights of a Mixed-Frequency Model}
\usage{
\method{weights}{mf_model}(object, indicator = NULL, ...)
}
\arguments{
\item{object}{A fitted \code{"mf_model"} object returned by \code{\link[=mf_model]{mf_model()}}.}

\item{indicator}{Optional indicator selector, either an indicator name or a
position. When supplied, the weights for that single indicator are
returned directly; when \code{NULL} (default), a named list covering every
indicator is returned.}

\item{...}{Unused.}
}
\value{
When \code{indicator} is \code{NULL}, a named list with one element per
indicator, each either a numeric weight vector or \code{NULL} for indicators
whose aggregator does not imply a fixed weight vector (\code{"unrestricted"}).
When \code{indicator} is supplied, that single element.
}
\description{
Extracts the within-period aggregation weights applied to each indicator.
For parametric aggregators (\code{"expalmon"}, \code{"beta"}) these are the weights
implied by the estimated parameters; for user-supplied numeric aggregators
they are the supplied weights.
}
\details{
The deterministic rules imply the fixed weight vectors of
\eqn{\tilde x_t = \sum_m w_m x_{t,m}}: \code{"mean"} gives \eqn{w_m = 1/M},
\code{"last"} gives \eqn{w_M = 1} and zero elsewhere, and \code{"sum"} gives
\eqn{w_m = 1}. These are returned alongside the estimated and
user-supplied weights, so the accessor reports the weights actually
applied whichever aggregator was chosen. \code{"unrestricted"} has no single
weight vector, because it estimates one coefficient per within-period
observation instead of aggregating; it returns \code{NULL}, as does an
indicator aligned with \code{indic_predict = "direct"}.
}
\examples{
gdp_growth <- tsbox::ts_pc(gdp)
gdp_growth <- tsbox::ts_na_omit(gdp_growth)
model <- mf_model(
  target = gdp_growth,
  indic = baro,
  indic_aggregators = "expalmon",
  h = 1
)

weights(model)
weights(model, indicator = 1)
}
\seealso{
\code{\link[=aggregation_parameters]{aggregation_parameters()}} for the underlying parametric
parameters, and \code{\link[=indicators]{indicators()}} for the available indicator names.
}
