% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forecast.R
\name{as.forecast}
\alias{as.forecast}
\alias{as.forecast.mf_model_forecast}
\title{Coerce a Mixed-Frequency Forecast to a forecast Object}
\usage{
as.forecast(object, ...)

\method{as.forecast}{mf_model_forecast}(object, ...)
}
\arguments{
\item{object}{A \code{"mf_model_forecast"} object returned by
\code{\link[=forecast.mf_model]{forecast.mf_model()}}.}

\item{...}{Unused.}
}
\value{
An object of class \code{"forecast"}, with \code{mean}, \code{lower}, \code{upper}, \code{x},
\code{fitted} and \code{residuals} stored as \code{\link[stats:ts]{stats::ts()}} objects.
}
\description{
Converts a \code{"mf_model_forecast"} object into a genuine \code{"forecast"} object
from the \pkg{forecast} package, so that functions such as
\code{\link[forecast:reexports]{forecast::accuracy()}} can be used on \code{bridgr} output.
}
\details{
Conversion requires a target frequency that \code{\link[stats:ts]{stats::ts()}} can
represent exactly, that is, a whole number of target periods per calendar
year. Annual, semi-annual, quarterly, bi-monthly and monthly targets
qualify; daily, weekly and sub-daily targets do not, and are rejected with
an informative error rather than silently approximated.
}
\examples{
gdp_growth <- tsbox::ts_pc(gdp)
gdp_growth <- tsbox::ts_na_omit(gdp_growth)
model <- mf_model(
  target = gdp_growth,
  indic = baro,
  indic_predict = "auto.arima",
  indic_aggregators = "mean",
  h = 1
)

converted <- as.forecast(forecast(model))
class(converted)
}
\seealso{
\code{\link[=forecast.mf_model]{forecast.mf_model()}} for the native forecast object, which
supports every target frequency.
}
